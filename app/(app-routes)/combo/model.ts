/**
 * Combo route types. The combo landing page renders a `Bundle` of `type: "combo"`;
 * the canonical types live in the shared bundle domain module.
 */
export type {
  Bundle,
  BundleTier,
  BundleTierItem,
  BundlePerk,
  BundleType,
  BundleCartComponent,
} from "@/lib/bundles/types";
