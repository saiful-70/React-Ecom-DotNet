/**
 * Combo route types. The combo landing page renders a `Bundle`; the canonical
 * types live in the shared bundle domain module.
 */
export type {
  Bundle,
  BundleSummary,
  BundleTier,
  BundleTierItem,
  BundlePerk,
  BundleValidationResult,
  ValidateBundleRequest,
  BundleCartComponent,
} from "@/lib/bundles/types";
